{
  "name": "Asistente - Noticias",
  "nodes": [
    {
      "parameters": {},
      "id": "trigger-manual",
      "name": "Ejecutar ahora",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        -1120,
        128
      ]
    },
    {
      "parameters": {
        "rule": {
          "interval": [
            {
              "triggerAtHour": 8
            }
          ]
        }
      },
      "id": "trigger-schedule",
      "name": "Cada mañana 08:00",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [
        -1120,
        320
      ]
    },
    {
      "parameters": {
        "jsCode": "// Lee el perfil del usuario (config/user_profile.json) y el catálogo\n// (config/modules.json), ambos montados en /files/config, y resuelve la\n// configuración concreta que consumen los nodos siguientes.\nconst fs = require('fs');\nconst DIR = '/files/config';\n\nlet user = { selections: {}, ubicacion_laboral: '' };\nlet modules = {};\ntry { user = JSON.parse(fs.readFileSync(DIR + '/user_profile.json', 'utf8')); } catch (e) {}\ntry { modules = JSON.parse(fs.readFileSync(DIR + '/modules.json', 'utf8')); } catch (e) {}\n\nconst pick = (dim) => Array.isArray(user.selections && user.selections[dim]) ? user.selections[dim] : [];\nconst find = (list, id) => (list || []).find((o) => o && o.id === id);\n\nconst idiomaId = pick('idioma')[0] || 'espanol';\nconst idioma = (find(modules.idioma, idiomaId) && find(modules.idioma, idiomaId).valores.codigo) || 'es';\n\nconst LOCALE = {\n  es: ['es', 'ES', 'ES:es'],\n  ca: ['ca', 'ES', 'ES:ca'],\n  en: ['en-US', 'US', 'US:en'],\n  fr: ['fr', 'FR', 'FR:fr'],\n  de: ['de', 'DE', 'DE:de'],\n};\nconst loc = LOCALE[idioma] || LOCALE.es;\nconst hl = loc[0], gl = loc[1], ceid = loc[2];\n\nconst cats = [...new Set(\n  pick('intereses_rss').flatMap((id) => {\n    const m = find(modules.intereses_rss, id);\n    return (m && m.valores && m.valores.categorias_feed) || [];\n  })\n)];\n\nconst query = cats.length ? '(' + cats.join(' OR ') + ')' : '(economía OR tecnología OR ciencia OR IA)';\nconst feed_url = 'https://news.google.com/rss/search?q=' + encodeURIComponent(query)\n  + '&hl=' + hl + '&gl=' + gl + '&ceid=' + encodeURIComponent(ceid);\n\nconst estiloId = pick('estilo_marca_personal')[0];\nconst em = find(modules.estilo_marca_personal, estiloId);\nconst tono = (em && em.valores && em.valores.tono) || 'Claro, objetivo y sin adornos.';\n\nreturn [{ json: { profile: {\n  idioma,\n  feed_url,\n  tono,\n  max_palabras: 80,\n  categorias: cats,\n  ubicacion: user.ubicacion_laboral || '',\n} } }];\n"
      },
      "id": "cargar-perfil",
      "name": "Cargar perfil",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -896,
        224
      ]
    },
    {
      "parameters": {
        "url": "={{ $json.profile.feed_url }}",
        "options": {}
      },
      "id": "rss-noticias",
      "name": "RSS - Noticias",
      "type": "n8n-nodes-base.rssFeedRead",
      "typeVersion": 1.1,
      "position": [
        -688,
        224
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const title = ($json.title || '').trim();\nconst link = $json.link || $json.guid || '';\nconst norm = title.toLowerCase()\n  .normalize('NFD').replace(/[\\u0300-\\u036f]/g, '')\n  .replace(/[^a-z0-9 ]/g, '').trim();\n\nreturn { json: {\n  title,\n  link,\n  item_key: 'news:' + norm,\n  source: 'news',\n  text: (title + ' ' + ($json.contentSnippet || $json.content || '')).slice(0, 2000),\n  isoDate: $json.isoDate || $json.pubDate || null,\n} };\n"
      },
      "id": "normalizar",
      "name": "Normalizar noticia",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -464,
        224
      ]
    },
    {
      "parameters": {
        "jsCode": "// Deduplicación persistente entre ejecuciones usando los datos estáticos\n// del workflow (se guardan en la BD interna de n8n). Sin tabla externa.\nconst store = $getWorkflowStaticData('global');\nconst seen = store.newsSeen || {};\nconst now = Date.now();\nconst TTL = 30 * 24 * 60 * 60 * 1000; // 30 días\n\nfor (const k of Object.keys(seen)) {\n  if (now - seen[k] > TTL) delete seen[k];\n}\n\nconst out = [];\nfor (const item of $input.all()) {\n  const key = item.json.item_key;\n  if (!key || key === 'news:' || seen[key]) continue;\n  seen[key] = now;\n  out.push({ json: item.json, pairedItem: item.pairedItem });\n}\nstore.newsSeen = seen;\nreturn out;\n"
      },
      "id": "deduplicar",
      "name": "Deduplicar noticias",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        -240,
        224
      ]
    },
    {
      "parameters": {
        "maxItems": 5
      },
      "id": "limitar",
      "name": "Máx. 5 por ejecución",
      "type": "n8n-nodes-base.limit",
      "typeVersion": 1,
      "position": [
        -16,
        224
      ]
    },
    {
      "parameters": {
        "mode": "runOnceForEachItem",
        "jsCode": "const esc = (s) => String(s == null ? '' : s)\n  .replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');\n\nlet data;\ntry {\n  const raw = ($json.candidates && $json.candidates[0] && $json.candidates[0].content\n    && $json.candidates[0].content.parts && $json.candidates[0].content.parts[0]\n    && $json.candidates[0].content.parts[0].text) || '{}';\n  data = JSON.parse(raw);\n} catch (e) {\n  throw new Error('Respuesta de Gemini no es JSON válido: ' + e.message);\n}\nif (!data.titulo || !data.resumen) {\n  throw new Error('JSON de Gemini incompleto (falta titulo o resumen)');\n}\n\nlet enlace = '';\ntry { enlace = $('Normalizar noticia').item.json.link || ''; } catch (e) { enlace = ''; }\n\nreturn { json: {\n  titulo: esc(data.titulo),\n  resumen: esc(data.resumen),\n  relevancia: data.relevancia || '',\n  enlace,\n} };\n"
      },
      "id": "validar",
      "name": "Validar JSON Gemini",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        640,
        224
      ]
    },
    {
      "parameters": {
        "chatId": "8319148167",
        "text": "={{ $json.texto }}",
        "additionalFields": {
          "appendAttribution": false,
          "parse_mode": "HTML"
        }
      },
      "type": "n8n-nodes-base.telegram",
      "typeVersion": 1.2,
      "position": [
        1472,
        224
      ],
      "id": "f26738fb-6f02-4ef5-aa5f-00ffea5a8247",
      "name": "Send a text message",
      "webhookId": "c6559a9f-ca4f-4a99-9a0d-cf64dfbbea54",
      "credentials": {
        "telegramApi": {
          "id": "ME0L9as09vfow89m",
          "name": "Telegram account"
        }
      }
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://generativelanguage.googleapis.com/v1beta/models/gemini-3.6-flash:generateContent",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "x-goog-api-key",
              "value": "{{ $env.GEMINI_API_KEY }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={\n  \"contents\": [\n    {\n      \"parts\": [\n        {\n          \"text\": {{ JSON.stringify(\"Resume esta noticia en máximo 80 palabras en español. Devuelve titulo, resumen, relevancia (alta/media/baja), prioridad (1-5) y enlace.\\n\\nTítulo: \" + $json.title + \"\\nEnlace: \" + $json.link + \"\\nContenido: \" + $json.text) }}\n        }\n      ]\n    }\n  ],\n  \"generationConfig\": {\n    \"responseMimeType\": \"application/json\",\n    \"responseSchema\": {\n      \"type\": \"OBJECT\",\n      \"properties\": {\n        \"titulo\": {\n          \"type\": \"STRING\"\n        },\n        \"resumen\": {\n          \"type\": \"STRING\"\n        },\n        \"relevancia\": {\n          \"type\": \"STRING\"\n        },\n        \"prioridad\": {\n          \"type\": \"INTEGER\"\n        },\n        \"enlace\": {\n          \"type\": \"STRING\"\n        }\n      },\n      \"required\": [\n        \"titulo\",\n        \"resumen\",\n        \"relevancia\",\n        \"prioridad\",\n        \"enlace\"\n      ]\n    }\n  }\n}",
        "options": {}
      },
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.3,
      "position": [
        192,
        224
      ],
      "id": "c190ef06-7d9f-4522-954c-696518fa3bd3",
      "name": "HTTP Request"
    },
    {
      "parameters": {
        "jsCode": "const items = $input.all().map(i => i.json);\nconst fecha = new Date().toLocaleDateString('es-ES', { \n  weekday: 'long', day: 'numeric', month: 'long' \n});\n\nconst bloques = items.map((n, i) => \n  `<b>${i + 1}. ${n.titulo}</b>\\n` +\n  `${n.resumen}\\n` +\n  `<a href=\"${n.enlace}\">🔗 Leer noticia</a>`\n).join('\\n\\n━━━━━━━━━━\\n\\n');\n\nconst texto = \n  `📰 <b>TUS NOTICIAS</b>\\n` +\n  `<i>${fecha}</i>\\n\\n` +\n  `━━━━━━━━━━\\n\\n` +\n  `${bloques}\\n\\n` +\n  `━━━━━━━━━━\\n` +\n  `<i>${items.length} noticias seleccionadas</i>`;\n\nreturn [{ json: { texto } }];"
      },
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        848,
        224
      ],
      "id": "9f1b9968-5b6f-464c-99cc-40b7d593d414",
      "name": "Code in JavaScript"
    }
  ],
  "pinData": {},
  "connections": {
    "Ejecutar ahora": {
      "main": [
        [
          {
            "node": "Cargar perfil",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cada mañana 08:00": {
      "main": [
        [
          {
            "node": "Cargar perfil",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Cargar perfil": {
      "main": [
        [
          {
            "node": "RSS - Noticias",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "RSS - Noticias": {
      "main": [
        [
          {
            "node": "Normalizar noticia",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Normalizar noticia": {
      "main": [
        [
          {
            "node": "Deduplicar noticias",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Deduplicar noticias": {
      "main": [
        [
          {
            "node": "Máx. 5 por ejecución",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Máx. 5 por ejecución": {
      "main": [
        [
          {
            "node": "HTTP Request",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Validar JSON Gemini": {
      "main": [
        [
          {
            "node": "Code in JavaScript",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "HTTP Request": {
      "main": [
        [
          {
            "node": "Validar JSON Gemini",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Code in JavaScript": {
      "main": [
        [
          {
            "node": "Send a text message",
            "type": "main",
            "index": 0
          }
        ]
      ]
    }
  },
  "active": false,
  "settings": {
    "executionOrder": "v1",
    "saveManualExecutions": true,
    "saveDataErrorExecution": "all",
    "saveDataSuccessExecution": "all"
  },
  "versionId": "f7c30c2b-0c9c-4699-a7c4-de496b1628f8",
  "meta": {
    "templateCredsSetupCompleted": true,
    "instanceId": "055f7ba4abc952d024eca101b02c7691b8cced7c815ac2a16117361afe9f5962"
  },
  "id": "0ikHqQCWMke67aoI",
  "tags": []
}